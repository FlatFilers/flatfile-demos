/**
 * Converts an error object into a plain object.
 * @param {Error} Error object.
 * @returns {Object}
 */
export declare const serializeError: ({ name, message, stack }: {
    name: any;
    message: any;
    stack: any;
}) => {
    name: any;
    message: any;
    stack: any;
};
/**
 * Converts a plain object into an error object.
 * @param {Object} Object with error properties.
 * @returns {Error}
 */
export declare const deserializeError: (obj: any) => Error;
